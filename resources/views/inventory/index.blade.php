<h1>Inventario</h1><form method="post" action="{{route('inventory.categories')}}">@csrf<input name="name" placeholder="Categoría"><button>Guardar categoría</button></form><form method="post" action="{{route('inventory.units')}}">@csrf<select name="catalog_item_id">@foreach($products as $p)<option value="{{$p->id}}">{{$p->name}}</option>@endforeach</select><input name="serial_number" placeholder="Número de serie"><button>Registrar pieza</button></form><table><thead><tr><th>Producto</th><th>Número de serie</th><th>Estado</th></tr></thead><tbody>@foreach($units as $u)<tr><td>{{$u->item?->name}}</td><td>{{$u->serial_number}}</td><td>{{$u->status}}</td></tr>@endforeach</tbody></table>
