<!doctype html><html lang="es"><head><meta charset="utf-8"><title>Catálogo | Comunitec</title></head><body><main><h1>Catálogo</h1><form method="POST" action="{{ route('catalog.store') }}">@csrf<select name="type"><option value="product">Producto</option><option value="service">Servicio</option></select><select name="category_id"><option value="">Sin categoría</option>@foreach($categories as $category)<option value="{{ $category->id }}">{{ $category->name }}</option>@endforeach</select><input name="name" placeholder="Nombre" required><input name="code" placeholder="Código" required><input name="brand" placeholder="Marca"><input name="model" placeholder="Modelo"><input name="unit" placeholder="Unidad" required><input name="price" type="number" step="0.01" required><input name="stock" type="number" min="0"><button>Guardar concepto</button></form><table><thead><tr><th>Código</th><th>Nombre</th><th>Categoría</th><th>Tipo</th><th>Stock</th></tr></thead><tbody>@foreach($items as $item)<tr><td>{{ $item->code }}</td><td>{{ $item->name }}</td><td>{{ $item->category_id ?? 'Sin categoría' }}</td><td>{{ $item->type }}</td><td>{{ $item->stock }}</td></tr>@endforeach</tbody></table></main></body></html>
